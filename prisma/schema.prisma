// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id             String          @id @default(cuid())
  name           String?
  email          String          @unique
  password       String
  isAdmin        Boolean         @default(false)
  createdAt      DateTime        @default(now()) @db.Timestamp(6)
  updatedAt      DateTime        @updatedAt @db.Timestamp(6)
  cheatsheetPages CheatsheetPage[]
}

model CheatsheetPage {
  id          String            @id @default(cuid())
  title       String
  description String?
  topic       String
  color       String            @default("#6366f1") // For acid watercolor theme
  order       Int               @default(0)
  isPublic    Boolean           @default(true)
  createdAt   DateTime          @default(now()) @db.Timestamp(6)
  updatedAt   DateTime          @updatedAt @db.Timestamp(6)
  authorId    String
  author      User              @relation(fields: [authorId], references: [id], onDelete: Cascade)
  entries     CheatsheetEntry[]

  @@map("cheatsheet_pages")
}

model CheatsheetEntry {
  id            String         @id @default(cuid())
  title         String
  content       String
  type          EntryType      @default(TERMINOLOGY)
  tags          String[]       @default([])
  order         Int            @default(0)
  isCodeSnippet Boolean        @default(false)
  language      String?        // For syntax highlighting
  createdAt     DateTime       @default(now()) @db.Timestamp(6)
  updatedAt     DateTime       @updatedAt @db.Timestamp(6)
  pageId        String
  page          CheatsheetPage @relation(fields: [pageId], references: [id], onDelete: Cascade)

  @@map("cheatsheet_entries")
}

enum EntryType {
  TERMINOLOGY
  FORMULA
  CODE_SNIPPET
  NOTE
  REFERENCE
}


